/*const {
    message
} = require('./mongoDB/models');

const {
    message
} = require('./sequalize/models')

*/

const {
    message
} = require('./cassandra/models')


module.exports = {
    message
}